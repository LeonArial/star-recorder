# ASR API 服务器依赖
# 用于 asr_api_server.py
#
# 本地开发（macOS/CPU）: pip install -r requirements.txt
# Docker/GPU 服务器: 使用 requirements-gpu.txt

# 深度学习框架（需单独安装）
# macOS: pip install torch torchaudio
# GPU: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# Web框架
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.9.0
eventlet>=0.33.0

# FunASR 语音识别
funasr>=1.0.0
modelscope>=1.9.0

# 音频处理
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.10.0

# HTTP请求（LLM调用）
requests>=2.31.0

# 数据处理
numpy>=1.24.0,<2.0.0

# FunASR额外依赖
# macOS/CPU 使用 onnxruntime，GPU 服务器使用 onnxruntime-gpu
onnxruntime>=1.15.0
kaldiio>=2.18.0
typeguard>=2.13.3
